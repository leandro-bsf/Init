import React from 'react';
import ReactDOM from 'react-dom';
import Rota from './Rota'; // Importe o componente Rotas

ReactDOM.render(
  <React.StrictMode>
    <Rota /> {/* Renderize o componente Rotas */}
  </React.StrictMode>,
  document.getElementById('root')
);